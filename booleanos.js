const isUserOnMajority = true;

// is, does, has

const disabled = true;

if (disabled) {
  console.log("Disabled");
}

const isDisabled = true;

if (isDisabled) {
  console.log("Disabled");
}

const userHasGroup = true;

// Booleans sempre como perguntas / semântico

const loading = false;
const isLoading = false;

const isUserProfileLoading = false;

// Evite variáveis genéricas: loading, disabled, data, fetching, list
